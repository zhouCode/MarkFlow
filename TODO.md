# TODO

- [ ] Export: PDF/HTML, with optional note inclusion modes
- [ ] Optimize the share-window chrome/layout to reduce wasted top-bar space from the OS title bar and in-app toolbar
- [ ] Fix share window layout so the presentation reflows automatically when the window is resized
- [x] Support YAML frontmatter titles and resolve Markdown image paths relative to the opened document in preview/share rendering
- [x] Keep invalid leading yaml-like text visible and load relative Markdown images through a restricted local asset protocol
- [x] Prefer OS-requested markdown files over the last persisted document when launching from file associations or right-click open
- [x] Auto-update strategy (manual check)
